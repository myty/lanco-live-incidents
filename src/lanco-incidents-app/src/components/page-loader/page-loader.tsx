import IconLoader from "components/icons/icon-refresh";
import React from "react";

const PageLoader: React.FC = () => {
  return (
    <div>
      <IconLoader animate={true} />
    </div>
  );
};

export default PageLoader;
